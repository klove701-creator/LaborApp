{
  "name": "labor-app",
  "version": "1.0.0",
  "description": "노무비 관리 시스템",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "gunicorn app:app --bind 0.0.0.0:$PORT",
    "dev": "python app.py"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "."
  },
  "author": "LaborApp Team",
  "license": "MIT"
}